const str ='google'
const str1 = str.split('')

const a = str1.filter((p,q,r)=>{
      return r.indexOf(p)!==qhhhhh
})
const b = a.map((r,s)=>{
     if(str1.includes(r)){
      console.log(r,s)
     }
})
{oo}